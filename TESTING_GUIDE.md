# 设备管理跳转功能测试指南

## 功能说明
从设备管理页面点击"监测数据"按钮，能够根据设备类型跳转到对应的监测页面，并自动选中对应的水库节点。

## 测试步骤

### 1. 准备测试数据
确保设备列表中的设备数据包含以下字段：
- `deviceCategory`: 设备类型（必须）
- `reservoirManagementId`: 水库ID（必须）
- `reservoirName`: 水库名称（可选）

### 2. 设备类型映射
- `env` → 环境监测页面 (`/main/gate/environment`)
- `stress` → 应力监测页面 (`/main/gate/stress`)
- `displacement` → 位移监测页面 (`/main/gate/displacement`)
- `pressure` → 渗压监测页面 (`/main/gate/seepage`)
- `seepage` → 渗流监测页面 (`/main/gate/flow`)

### 3. 测试场景

#### 场景1：正常跳转
1. 打开设备管理页面
2. 找到包含完整数据的设备记录
3. 点击"监测数据"按钮
4. 验证：
   - 页面正确跳转到对应的监测页面
   - 左侧树形菜单选中了正确的水库节点
   - 显示成功提示消息

#### 场景2：数据不完整
1. 找到缺少`deviceCategory`或`reservoirManagementId`的设备
2. 点击"监测数据"按钮
3. 验证：
   - 显示错误提示"数据不完整无法跳转"
   - 页面不发生跳转

#### 场景3：未知设备类型
1. 修改某个设备的`deviceCategory`为未知类型（如`unknown`）
2. 点击"监测数据"按钮
3. 验证：
   - 显示错误提示"未知的设备类型"
   - 页面不发生跳转

### 4. 控制台日志检查
打开浏览器开发者工具，查看控制台输出：

#### 设备管理页面日志
```
=== 开始跳转监测数据页面 ===
设备信息: {设备对象}
设备类型: env
水库ID: 123
目标路由: environment
设置选中节点到store...
Store中当前选中节点: {节点对象}
开始页面跳转...
页面跳转成功
```

#### DamLayout页面日志
```
=== DamLayout组件挂载 ===
当前store中的选中节点: {节点对象}
=== 处理节点选中逻辑 ===
当前store中的选中节点: {节点对象}
树数据是否已加载: true
树组件是否已准备: true
检测到预设选中节点，尝试选中: {节点对象}
根据ID选中节点成功: {完整节点对象}
```

### 5. 对比测试

#### 手动进入监测页面
1. 直接点击左侧菜单进入监测页面
2. 验证：默认选中第一个水库节点

#### 跳转进入监测页面
1. 从设备管理页面跳转进入
2. 验证：选中对应的水库节点（而非第一个）

## 预期结果
- ✅ 跳转功能正常工作
- ✅ 树节点正确选中
- ✅ 错误情况正确处理
- ✅ 控制台日志清晰可读
- ✅ 用户体验良好

## 故障排除

### 问题1：树节点没有选中
- 检查`reservoirManagementId`是否与树数据中的`id`匹配
- 检查`selectNodeById`是否被正确调用
- 检查是否被`selectFirstNode`覆盖

### 问题2：页面跳转失败
- 检查路由配置是否正确
- 检查设备类型映射是否完整
- 检查浏览器控制台错误信息

### 问题3：数据不完整错误
- 检查API返回的设备数据结构
- 确认`deviceCategory`和`reservoirManagementId`字段存在且非空 