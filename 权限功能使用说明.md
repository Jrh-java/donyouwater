# 菜单按钮权限控制功能使用说明

## 概述

本项目已集成完整的菜单按钮权限控制功能，支持基于用户角色的细粒度权限管理。系统通过权限指令 `v-privilege` 实现前端权限控制，权限数据在用户登录后一次性加载并存储在 Pinia store 中，确保不同用户只能看到和操作其有权限的功能。

## 权限指令 `v-privilege`

### 基本用法

```vue
<!-- 只有拥有指定权限的用户才能看到此按钮 -->
<el-button v-privilege="'reservoir:add'" type="primary">
  添加水库
</el-button>

<!-- 支持多个权限，满足其中任一即可 -->
<el-button v-privilege="['user:add', 'user:edit']" type="success">
  用户管理
</el-button>
```

### 权限值格式

- **字符串形式**: `'module:action'` (如: `'reservoir:add'`, `'user:delete'`)
- **数组形式**: `['permission1', 'permission2']` (满足其中任一权限即可)

### 工作原理

1. 用户登录成功后，一次性调用 `queryMenuTree` 接口获取菜单权限数据
2. 从菜单树中提取所有功能点(menuType='F')的权限字符串并存储在 Pinia store 中
3. 权限指令从 Pinia store 中同步获取权限数据进行检查
4. 如果用户没有指定权限，元素会被从DOM中移除
5. 权限数据持久化到 localStorage，页面刷新后自动恢复
6. 用户登出时自动清除权限数据
7. 所有权限（包括管理员权限）由后端统一处理

## 权限数据获取机制

### Pinia Store 驱动的权限系统

权限数据在用户登录后加载到 Pinia store 中，权限指令从 store 中同步获取：

```javascript
// 在 Pinia store 中加载权限数据
const store = useStore();
await store.loadUserPermissions();

// 权限指令从 store 中获取权限数据
const userPermissions = store.userPermissions;
```

### 权限存储机制

- 权限数据在登录时一次性加载到 Pinia store
- 数据同时持久化到 localStorage，页面刷新后自动恢复
- 用户登出时自动清除 store 和 localStorage 中的权限数据
- 避免了每次权限检查都调用接口的性能问题

```javascript
// Pinia store 中的权限管理
const store = useStore();

// 检查权限
const hasPermission = store.hasPermission('reservoir:add');

// 清除权限（登出时自动调用）
store.clearUserPermissions();
```



### 菜单数据结构处理

Pinia store 会自动处理从接口获取的菜单数据：

1. **递归遍历菜单树**: 深度优先遍历整个菜单结构
2. **提取功能点权限**: 筛选出 `menuType === 'F'` 且 `status === 'T'` 且有 `permission` 字段的功能点
3. **权限列表生成**: 将所有有效的权限字符串收集到数组中
4. **持久化存储**: 将权限列表存储到 Pinia store 和 localStorage 中

### 数据流程

```
用户登录 → 调用 queryMenuTree 接口 → 解析菜单树 → 提取权限点 → 存储到 Pinia store → 权限指令从 store 获取 → 权限验证 → 显示/隐藏元素
```

### 本地存储

相关数据的本地存储：

- `USER_INFO`: 用户基本信息（包含 administratorFlag）
- `userPermissions`: 用户权限列表，存储在 localStorage 中，页面刷新后自动恢复到 Pinia store

## 权限验证逻辑

### 验证流程

1. **权限数据获取**: 从 Pinia store 中同步获取用户权限数据
2. **权限匹配**: 支持字符串和数组两种权限格式的匹配
3. **元素控制**: 根据权限结果显示或隐藏元素
4. **错误处理**: store 获取失败时，为安全起见隐藏元素

### 核心代码

```javascript
export function privilegeDirective(el, binding) {
  try {
    // 从 Pinia store 中同步获取用户权限列表
    const userPermissions = getUserPermissions();
    
    const requiredPermission = binding.value;
    let hasPermission = false;
    
    if (Array.isArray(requiredPermission)) {
      // 数组形式：满足其中任一权限即可
      hasPermission = requiredPermission.some(permission => 
        userPermissions.includes(permission)
      );
    } else {
      // 字符串形式：检查单个权限
      hasPermission = userPermissions.includes(requiredPermission);
    }
    
    // 如果没有权限，删除节点
    if (!hasPermission) {
      if (el && el.parentNode) {
        el.parentNode.removeChild(el);
      }
      return false;
    }
    
    return true;
  } catch (error) {
    console.error('权限检查失败:', error);
    // 出错时为安全起见，隐藏元素
    if (el && el.parentNode) {
      el.parentNode.removeChild(el);
    }
    return false;
  }
}
```

## 登录流程说明

### 登录成功处理

在 `login.vue` 中，登录成功后的处理流程：

1. 设置 token 和 roleId 到 Pinia store
2. 设置用户基本信息到 user store
3. 一次性加载用户权限到 Pinia store
4. 跳转到主页面

```javascript
// 登录成功后的处理
if (res && res.token) {
  // 设置基本信息到 Pinia store
  store.setToken(res.token);
  store.setRoleId(res.roleId);
  store.login(username.value);
  
  // 设置用户信息到 user store
  const menuRes = await queryMenuTree();
  if (menuRes) {
    userStore.setUserLoginInfo({
      token: res.token,
      employeeId: res.employeeId,
      actualName: res.actualName,
      administratorFlag: res.administratorFlag,
      menuTree: menuRes
      // ... 其他用户信息字段
    });
    
    // 加载用户权限到 Pinia store
    await store.loadUserPermissions();
    console.log('用户权限已加载到Pinia store');
  }
  
  // 跳转到主页
  router.push('/viewer-fullscreen');
}
```

### 权限数据获取时机

- 权限数据在用户登录成功后立即加载到 Pinia store
- 数据持久化到 localStorage，页面刷新后自动恢复
- 权限指令从 store 中同步获取，无需等待接口调用
- 提升了权限检查的性能和用户体验

## 已应用的权限控制

### 水库管理页面

在 `/src/view/reservoir.vue` 中，"添加水库"按钮已应用权限控制：

```vue
<el-button 
  v-privilege="'gate:overviewer:add'"
  type="primary" 
  @click="handleAdd"
>
  添加水库
</el-button>
```

只有拥有 `gate:overviewer:add` 权限的用户才能看到此按钮。权限数据会从 `queryMenuTree` 接口实时获取。

## 技术实现细节

### 权限指令注册

在 `main.ts` 中注册异步权限指令：

```javascript
import { privilegeDirective } from '@/directives/privilege';

app.directive('privilege', {
  async mounted(el, binding) {
    await privilegeDirective(el, binding);
  },
  async updated(el, binding) {
    // 当绑定值更新时也需要重新检查权限
    await privilegeDirective(el, binding);
  }
});
```

### 权限缓存机制

```javascript
// 权限缓存，避免重复请求
let permissionCache = null;
let cacheTime = 0;
const CACHE_DURATION = 5 * 60 * 1000; // 5分钟缓存

// 获取用户权限列表
async function getUserPermissions() {
  const now = Date.now();
  
  // 如果缓存有效，直接返回缓存
  if (permissionCache && (now - cacheTime) < CACHE_DURATION) {
    return permissionCache;
  }
  
  // 调用接口获取最新权限数据
  const menuData = await queryMenuTree();
  const permissions = flattenMenuPermissions(menuData);
  
  // 更新缓存
  permissionCache = permissions;
  cacheTime = now;
  
  return permissions;
}
```

### 菜单权限提取

```javascript
// 扁平化菜单树，提取所有权限点
function flattenMenuPermissions(menuTree) {
  const permissions = [];
  
  function traverse(menus) {
    if (!Array.isArray(menus)) return;
    
    menus.forEach(menu => {
      // 如果是功能点(F)且有权限字符串，则添加到权限列表
      if (menu.menuType === 'F' && menu.permission && menu.status === 'T') {
        permissions.push(menu.permission);
      }
      
      // 递归处理子菜单
      if (menu.children && menu.children.length > 0) {
        traverse(menu.children);
      }
    });
  }
  
  traverse(menuTree);
  return permissions;
}
```

## 扩展使用

### 在组件中使用

```vue
<template>
  <div>
    <!-- 按钮权限控制 -->
    <el-button v-privilege="'user:add'" @click="addUser">
      添加用户
    </el-button>
    
    <!-- 表格操作列权限控制 -->
    <el-table-column label="操作">
      <template #default="scope">
        <el-button 
          v-privilege="'user:edit'"
          size="small" 
          @click="editUser(scope.row)"
        >
          编辑
        </el-button>
        <el-button 
          v-privilege="'user:delete'"
          size="small" 
          type="danger"
          @click="deleteUser(scope.row)"
        >
          删除
        </el-button>
      </template>
    </el-table-column>
    
    <!-- 菜单项权限控制 -->
    <el-menu>
      <el-menu-item v-privilege="'system:config'" index="config">
        系统配置
      </el-menu-item>
    </el-menu>
    
    <!-- 支持数组权限（满足任一即可） -->
    <el-button v-privilege="['data:export', 'data:download']" @click="exportData">
      导出数据
    </el-button>
  </div>
</template>
```

### 编程式权限检查

```javascript
import { queryMenuTree } from '@/api/menu';

// 获取用户权限列表
async function getUserPermissions() {
  try {
    const menuData = await queryMenuTree();
    const permissions = [];
    
    function extractPermissions(menus) {
      menus.forEach(menu => {
        if (menu.menuType === 'F' && menu.permission && menu.status === 'T') {
          permissions.push(menu.permission);
        }
        if (menu.children && menu.children.length > 0) {
          extractPermissions(menu.children);
        }
      });
    }
    
    extractPermissions(menuData);
    return permissions;
  } catch (error) {
    console.error('获取权限失败:', error);
    return [];
  }
}

// 检查单个权限
async function hasPermission(permission) {
  const permissions = await getUserPermissions();
  return permissions.includes(permission);
}

// 在方法中使用
const handleOperation = async () => {
  if (!(await hasPermission('data:export'))) {
    ElMessage.error('您没有数据导出权限');
    return;
  }
  // 执行操作
};
```

**注意**：编程式权限检查不包含管理员权限判断，所有权限由后端统一处理。

### 权限数据管理

权限数据现在统一由 Pinia store 管理，具有以下特点：

- 登录时自动加载权限数据
- 数据持久化到 localStorage
- 登出时自动清理权限数据
- 无需手动管理缓存

```javascript
// 权限数据由 Pinia store 自动管理
// 登录时：store.loadUserPermissions()
// 登出时：store.clearUserPermissions()
// 检查权限：store.hasPermission(permission)
```

## 注意事项

1. **权限字符串格式**: 建议使用 `模块:操作` 的格式，如 `user:add`、`system:config`
2. **后端鉴权**: 所有权限（包括管理员权限）由后端统一处理，前端不再进行管理员权限检查
3. **权限缓存**: 权限数据会缓存到内存中5分钟，登出时会自动清除
4. **异步权限**: 权限指令是异步的，首次加载可能有短暂延迟
5. **接口依赖**: 权限功能依赖 `queryMenuTree` 接口，确保接口正常可用
6. **网络异常**: 网络异常时，为安全起见会隐藏所有需要权限的元素
7. **权限更新**: 权限变更后，缓存会在5分钟后自动过期，或手动清除缓存
8. **用户切换**: 用户登出或登录时会自动清除权限缓存，确保不同用户间权限隔离

## 故障排除

### 权限不生效

1. 检查权限字符串是否正确
2. 确认 `queryMenuTree` 接口是否正常返回数据
3. 检查菜单数据中功能点的 `permission` 字段
4. 查看浏览器控制台是否有错误信息
5. 检查网络连接是否正常

### 用户切换权限问题

1. **问题**: 用户退出后登录其他用户，仍保留上一个用户的按钮权限
2. **原因**: 权限缓存未及时清除
3. **解决方案**: 已在 `logout()` 和 `setUserLoginInfo()` 方法中自动清除权限缓存
4. **验证**: 切换用户后权限应立即更新

### 权限指令异步加载问题

1. **问题**: 组件首次加载时，无权限的按钮会短暂显示，然后才被隐藏
2. **原因**: 权限检查是异步的，在检查完成前元素已经渲染
3. **解决方案**: 权限指令在检查期间先隐藏元素，检查完成后根据结果决定显示或移除
4. **效果**: 避免无权限元素的闪现问题

### 权限数据为空

1. 确认 `queryMenuTree` 接口返回数据格式正确
2. 检查菜单数据中是否包含功能点(menuType='F')
3. 确认功能点的 `status` 字段是否为 'T'
4. 确认功能点是否有 `permission` 字段且不为空



### 权限数据问题

1. 检查 Pinia store 中的权限数据是否正确加载
2. 确认 localStorage 中的权限数据是否存在
3. 确认用户登出时权限数据是否被清除
4. 用户登出和登录时会自动清除权限数据

### 接口调用失败

1. 检查 `queryMenuTree` 接口是否正常
2. 确认用户 token 是否有效
3. 检查网络连接和服务器状态
4. 查看接口返回的错误信息